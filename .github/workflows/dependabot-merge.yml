---
name: 'Pipeline - Dependabot Auto Merge'

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  automerge:
    runs-on: ubuntu-latest

    permissions:
      pull-requests: write
      contents: write

    steps:
      - name: Dependabot Auto Merge
        uses: fastify/github-action-merge-dependabot@d88a8705233a11bd65e08c7b22502e9360bd0e4c
